# auto-generated by grc.converter

id: burst_preamble_insert
label: Burst Preamble Insert
category: '[sdlp]'

parameters:
-   id: preamble
    label: Preamble
    dtype: byte

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: exec(open("/home/spacecraft/cygnus/gnuradio/preamble_insert_jbh_jej.py").read())
    make: preamble_insert(${preamble})

documentation: ""

file_format: 1

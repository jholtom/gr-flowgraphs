# auto-generated by grc.converter

id: spp_destruct_packet
label: destruct_packet
category: '[spp]'

parameters:
-   id: elysiumapid
    label: Radio APID
    dtype: int
-   id: sf2cmdapid
    label: SF2CMD APID
    dtype: int
-   id: tftpapid
    label: TFTP APID
    dtype: int
-   id: beaconapid
    label: Beacon APID
    dtype: int
-   id: encrypt
    label: Encryption Mode
    dtype: int
-   id: enckey
    label: Encryption Key
    dtype: string

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: elysium
-   domain: message
    id: sf2cmd
-   domain: message
    id: tftp
-   domain: message
    id: beacon
-   domain: message
    id: out

templates:
    imports: exec(open("/home/spacecraft/cygnus/gnuradio/gr-spp/python/destruct_packet.py").read())
    make: destruct_packet(${elysiumapid}, ${sf2cmdapid}, ${tftpapid}, ${beaconapid},
        ${encrypt}, ${enckey})

documentation: |-
    This block is designed to recieve a Space Packet Protocol (SPP) packet that has been generated using the create_packet block from the SPP library. This recieved packet will be deconstructed to then return the payload data to the user via the output terminal.

        Input: This should be of type message and contain a single SPP packet that the user desires to deconstruct to retrieve the payload data.
        Output: This is of message type and contains the payload data from inside the recieved packet.

file_format: 1

# auto-generated by grc.converter

id: usrp_pack
label: usrp_pack
category: '[sdlp]'

parameters:
-   id: samples_per_bit
    label: samples_per_bit
    dtype: int
-   id: bits_per_symbol
    label: bits_per_symbol
    dtype: int

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: execfile("/home/spacecraft/cygnus/gnuradio/gr-usrp_pack/python/usrp_packer.py")
    make: usrp_packer(${samples_per_bit},${bits_per_symbol})

documentation: |-
    This packs up blocks for USRP usage and alignment.

file_format: 1
